# MEMORY.md

## User Profile
- Primary language: Chinese.
- Primary platform in use: OpenClaw Manager on macOS.

## Environment Notes
- Main OpenClaw config path: `/Users/zhangxh/.openclaw/openclaw.json`.
- Workspace path: `/Users/zhangxh/.openclaw/workspace`.

## Decisions
- Telegram access policy is restricted to paired DM + allowlist mode.
- Memory plugin slot uses `memory-core`.
- Feishu plugin is enabled for later channel binding.
